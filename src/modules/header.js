const LOGIN_ON = 'LOGIN_ON'
const LOGIN_OUT = 'LOGIN_OUT'

const initialState = {
    login: false
}

export default function(state=initialState, action) {
    switch(action.type) {
        
        default:
            return state
    }
}